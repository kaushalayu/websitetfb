# Static assets — serve as-is (do NOT rewrite to index.html)
/assets/*    /assets/:splat   200
/uploads/*   /uploads/:splat  200
/favicon.svg /favicon.svg     200
/icons.svg   /icons.svg       200
/robots.txt  /robots.txt      200

# Dynamic sitemap from backend
/sitemap.xml https://adi-backend-vb6n.onrender.com/sitemap.xml   200

# SPA fallback — only non-file routes go to index.html
/*           /index.html      200
